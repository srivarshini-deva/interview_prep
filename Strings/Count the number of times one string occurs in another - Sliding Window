int countOccurrences(string text, string pattern) {
    int count = 0;
    int n = text.length();
    int m = pattern.length();

    for (int i = 0; i <= n - m; i++) {
        if (text.substr(i, m) == pattern) {
            count++;
        }
    }
    return count;
}
