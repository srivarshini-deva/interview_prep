Time & Space

Time: O(n)

Space: O(1)

  for (int i = 0; i < s.length(); i++) {
        if (isupper(s[i]))
            s[i] = tolower(s[i]);
        else if (islower(s[i]))
            s[i] = toupper(s[i]);
    }

Time: O(n)

Space: O(1)

 for (char &c : s) {
        if (c >= 'A' && c <= 'Z')
            c = c + 32;
        else if (c >= 'a' && c <= 'z')
            c = c - 32;
}

A - Z 65 - 90
a - z 97 - 122
