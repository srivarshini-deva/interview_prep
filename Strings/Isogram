bool isIsogram(string s) {
    int freq[26] = {0};

    for (char c : s) {
        if (isalpha(c)) {
            c = tolower(c);
            if (freq[c - 'a'] > 0)
                return false;
            freq[c - 'a']++;
        }
    }
    return true;
}
